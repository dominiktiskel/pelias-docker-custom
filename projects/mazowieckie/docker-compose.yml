networks:
  pelias_mazowieckie:
    driver: bridge
services:
  libpostal:
    image: pelias/libpostal-service
    container_name: mazowieckie_libpostal
    user: "${DOCKER_USER}"
    restart: always
    ports: [ "127.0.0.1:24400:4400" ]
    networks: [ "pelias_mazowieckie" ]
  schema:
    image: pelias/schema:master
    container_name: mazowieckie_schema
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
    networks: [ "pelias_mazowieckie" ]
  api:
    image: tiskel/pelias-api:v1.0.4
    container_name: mazowieckie_api
    user: "${DOCKER_USER}"
    restart: always
    environment: [ "PORT=4000" ]
    ports: [ "0.0.0.0:24000:4000" ]
    volumes:
      - "./pelias.json:/code/pelias.json"
    networks: [ "pelias_mazowieckie" ]
  placeholder:
    image: pelias/placeholder:master
    container_name: mazowieckie_placeholder
    user: "${DOCKER_USER}"
    restart: always
    environment: [ "PORT=4100" ]
    ports: [ "127.0.0.1:24100:4100" ]
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
      - "./blacklist/:/data/blacklist"
    networks: [ "pelias_mazowieckie" ]
  whosonfirst:
    image: pelias/whosonfirst:master
    container_name: mazowieckie_whosonfirst
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  openstreetmap:
    image: tiskel/openstreetmap:v2.8.3
    container_name: mazowieckie_openstreetmap
    user: "${DOCKER_USER}"
    environment:
      NODE_OPTIONS: "--max-old-space-size=16384"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  openaddresses:
    image: pelias/openaddresses:master
    container_name: mazowieckie_openaddresses
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  csv-importer:
    image: pelias/csv-importer:master
    container_name: mazowieckie_csv_importer
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
      - "./blacklist/:/data/blacklist"
    networks: [ "pelias_mazowieckie" ]
  transit:
    image: pelias/transit:master
    container_name: mazowieckie_transit
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  polylines:
    image: pelias/polylines:master
    container_name: mazowieckie_polylines
    user: "${DOCKER_USER}"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  interpolation:
    image: pelias/interpolation:master
    container_name: mazowieckie_interpolation
    user: "${DOCKER_USER}"
    restart: always
    environment: [ "PORT=4300" ]
    ports: [ "127.0.0.1:24300:4300" ]
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  pip:
    image: pelias/pip-service:master
    container_name: mazowieckie_pip
    user: "${DOCKER_USER}"
    restart: always
    environment: [ "PORT=4200" ]
    ports: [ "127.0.0.1:24200:4200" ]
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "${DATA_DIR}:/data"
    networks: [ "pelias_mazowieckie" ]
  elasticsearch:
    image: pelias/elasticsearch:7.17.27
    container_name: mazowieckie_elasticsearch
    user: "${DOCKER_USER}"
    restart: always
    ports: [ "127.0.0.1:29200:9200", "127.0.0.1:29300:9300" ]
    volumes:
      - "./elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro"
      - "${DATA_DIR}/elasticsearch:/usr/share/elasticsearch/data"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    cap_add: [ "IPC_LOCK" ]
    security_opt:
      - seccomp=unconfined
    networks: [ "pelias_mazowieckie" ]
  fuzzy-tester:
    image: pelias/fuzzy-tester:master
    container_name: mazowieckie_fuzzy_tester
    user: "${DOCKER_USER}"
    restart: "no"
    command: "--help"
    volumes:
      - "./pelias.json:/code/pelias.json"
      - "./test_cases:/code/pelias/fuzzy-tester/test_cases"
    networks: [ "pelias_mazowieckie" ]
